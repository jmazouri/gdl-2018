using UnityEngine;

namespace Player
{
    public class MovementConfig : MonoBehaviour {

        // Use this for initialization
        void Start () {
		
        }
	
        // Update is called once per frame
        void Update () {
		
        }
    }
}
